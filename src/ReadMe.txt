npm installs:

-after npm install just replace
"node_modules/@expo/ex-navigation/src/drawer" folder with "src/helper/ReplaceDrawerinExpoNPM" to run the drawer with existing UI changes and click events